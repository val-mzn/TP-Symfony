{% extends 'base.html.twig' %}

{% block title %}Edit Location{% endblock %}

{% block stylesheets %}
<style>
    #location div {
        margin: 0.2rem 1rem 0.2rem 1rem;
        height: 2rem;
    }

    #location div label {
        float: left;
        width: 7rem;
    }

    #location div input {
        float: right;
        width: 15rem;
    }
</style>
{% endblock %}

{% block body %}
<a class="btn btn-secondary" href="{{ path('location_index') }}" style="margin-bottom: 1rem;">Annuler</a>

<div class="card text-white bg-dark mb-3" style="width: 35rem;">
    <ul class="list-group list-group-flush">
        <h5 class="card-header">Client a modifier</h5>
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}

        </div>
        <div class="card-footer">
            <button class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
            {{ form_end(form) }}
            {{include('location/_delete_form.html.twig') }}
        </div>
    </ul>
</div>
{% endblock %}